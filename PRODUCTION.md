# ClickHouse 프로덕션 운영 가이드

이 문서는 [GUIDE.md](./GUIDE.md)(로컬 kind 랩 재현 가이드)와 성격이 다릅니다.
GUIDE.md가 "이 랩을 어떻게 다시 만드는가"를 다룬다면, 이 문서는 **실제 운영
환경에서 ClickHouse를 고성능·고가용으로 운영하려면 무엇을 설정/점검해야
하는가**를 다룹니다.

가능한 곳에서는 이 랩의 실험을 통해 **직접 확인한 사실**을 근거로 들었습니다
(예: GUIDE.md N절 참조). 그렇지 않은 항목은 ClickHouse 공식 문서
(clickhouse.com/docs)와 Altinity 문서(docs.altinity.com)를 근거로 했습니다.

---

## 1. 하드웨어 & 스토리지

- **디스크**: NVMe/SSD 필수. ClickHouse는 랜덤 I/O가 아니라 대용량 순차 스캔이
  주 워크로드지만, 병합(merge)과 mutation은 지속적인 쓰기 부하를 만듭니다. 특히
  **Keeper의 changelog/snapshot 디렉터리는 매 쓰기마다 fsync**하므로(기본
  `force_sync=on`) 느린 디스크는 곧바로 쓰기 지연으로 이어집니다.
- **네트워크 스토리지 지양**: NFS/EBS류 네트워크 블록 스토리지는 지연시간이
  들쭉날쭉해 머지 스케줄러와 Keeper 양쪽에 안 좋습니다. 로컬 NVMe가 기본값이어야
  합니다.
- **CPU/메모리**: 컬럼 압축 해제와 집계가 CPU를 많이 쓰므로 코어 수가 중요합니다.
  메모리는 `max_server_memory_usage_to_ram_ratio`(기본 0.9)로 서버 전체 메모리
  상한을 잡되, **공유 호스트/쿠버네티스 환경에서는 0.8 정도로 낮춰 OS 페이지
  캐시·Keeper 클라이언트 등에 여유를 남기는 것**이 일반적인 권장입니다.
- **랩에서 확인한 것**: 우리 kind 랩(Colima VM 6vCPU/16GB, 12개 CH 파드가 물리
  코어를 공유)에서 `max_parallel_replicas`를 켰더니 오히려 QPS가 떨어졌습니다
  (GUIDE.md 15절). "병렬 리플리카"도 결국 물리 코어가 있어야 의미가 있다는
  실증 사례입니다 — 컨테이너 밀도를 과도하게 높이면 이런 기능들의 효과가
  사라지거나 역효과가 날 수 있습니다.

## 2. ClickHouse Keeper 운영

- **최소 3노드, 홀수 개수만**: 과반 합의(Raft)를 위해 3 또는 5. 5는 동시 2노드
  장애까지 버티지만 커밋 지연이 늘어나므로, 그 정도의 내구성이 꼭 필요한 게
  아니면 3을 권장합니다.
- **ClickHouse 데이터 노드와 별도 호스트에 배치**: 공식 문서는 프로덕션에서
  Keeper 전용 호스트를 강력히 권장합니다. Keeper가 데이터 노드와 자원을
  공유하면, 대량 쿼리로 인한 CPU/메모리 압박이 Raft 합의 지연(→ 쓰기 지연)으로
  전이될 수 있습니다.
- **안티어피니티(anti-affinity) 필수**: Keeper 3노드가 물리적으로 2:1로
  쏠려 있으면, 노드 하나 장애만으로도 과반을 잃을 수 있습니다.
  > ⚠️ **우리 랩에서 실제로 이 상태였습니다.** GUIDE.md 17절의 네트워크 파티션
  > 실험에서 `chk-chk-keeper-0-0`과 `-0-1`이 우연히 같은 워커 노드에 떠 있었고,
  > 이 덕분에(?) 깔끔한 2:1 스플릿 실험이 가능했습니다 — 뒤집어 말하면, 그
  > 워커 노드 하나가 죽었다면 곧바로 쿼럼을 잃는 구조였다는 뜻입니다.
  > 프로덕션에서는 반드시 `podAntiAffinity`(K8s) 또는 별도 랙/AZ 배치로 이런
  > 쏠림을 막아야 합니다.
- **쿼럼 상실 시 동작을 미리 알아둘 것**: Keeper 과반을 잃으면 `ReplicatedMergeTree`
  로의 쓰기가 **에러 없이 그냥 멈춥니다**(쿼럼 복구를 기다림). `Distributed`
  테이블 INSERT는 기본이 비동기라 겉보기엔 성공한 것처럼 보이지만 실제로는
  큐에 쌓이기만 합니다 — 알람을 "쓰기 실패"가 아니라 "쓰기 지연/큐 적체"
  관점으로 설계해야 합니다 (GUIDE.md 10절에서 직접 확인).
- **네트워크 파티션은 Raft가 스스로 치유합니다**: 프로세스가 죽는 장애와 달리,
  네트워크만 끊겼다 복구되는 경우는 사람이 개입하지 않아도 수 초 안에 재합류
  합니다 — 단, 파티션 도중 소수파가 자신을 여전히 "리더"라고 오인하는 과도기
  구간이 존재할 수 있다는 점(하트비트 타임아웃 전)도 확인했습니다 (GUIDE.md
  17절). 모니터링에서 Keeper의 `mntr`/`zk_server_state`를 지속 관찰하는 게
  중요한 이유입니다.

## 3. 스키마 설계

- **`ORDER BY` 키가 곧 성능**: 자주 필터링/정렬하는 컬럼을 앞에 두세요. 한 번
  정하면 되돌리기 비싸므로(테이블 재작성 필요) 초기 설계에 시간을 들이세요.
- **파티셔닝은 "적당히"**: 파티션 키(보통 날짜)는 오래된 데이터를 통째로
  버리기 위한 것이지, 세밀한 필터링용이 아닙니다. 파티션을 지나치게 세분화하면
  파티션마다 별도의 파트 세트가 생겨 "파트가 너무 많음" 문제를 스스로
  만듭니다.
- **`LowCardinality(String)`**: 카디널리티가 낮은 문자열 컬럼(상태값, 국가
  코드 등)에는 거의 항상 이득입니다.
- **Wide vs Compact 파트**: `min_bytes_for_wide_part`(기본 10MB),
  `min_rows_for_wide_part`(기본 0, 즉 바이트 기준이 우선)로 작은 파트는
  컬럼별 파일 대신 압축 포맷(compact)으로 저장됩니다. 소량·자주 쓰는 테이블
  (예: 우리 랩의 `ttl_demo`처럼 몇 행만 있는 테이블)에서는 이 임계값이 실제
  동작에 영향을 줍니다 — GUIDE.md 16절에서 확인했듯, 파트가 1개뿐인 작은
  테이블은 `OPTIMIZE ... FINAL`조차 "이미 최적화됨"으로 건너뛸 수 있으니
  이런 테이블의 TTL/정리는 `MATERIALIZE TTL`로 명시적으로 강제하는 걸
  권장합니다.

## 4. 인서트(쓰기) 전략

**"많은 작은 INSERT"는 ClickHouse의 가장 흔한 프로덕션 사고 원인입니다.** 각
INSERT는 최소 1개의 파트를 만들고, 파트가 너무 많아지면 서버가 스스로
브레이크를 겁니다:

- `parts_to_delay_insert = 1000` — 파티션당 파트가 이 수를 넘으면 INSERT가
  인위적으로 느려짐(경고 신호)
- `parts_to_throw_insert = 3000` — 이 수를 넘으면 INSERT가 **거부**됨(장애)

권장 순서:
1. **클라이언트 사이드 배치가 여전히 최선**: 가능하면 1,000~100,000행 단위로
   모아서 INSERT하세요. 공식 문서도 이 방법을 최우선으로 권장합니다.
2. **배치가 어려우면 `async_insert=1, wait_for_async_insert=1`**: 서버가
   여러 소스의 INSERT를 모아 하나의 파트로 합쳐줍니다. `wait_for_async_insert=0`
   은 클라이언트가 실제 반영 전에 리턴받아 **에러를 조용히 삼킬 수 있어
   위험**하다고 공식 문서가 명시하니 프로덕션에서는 피하세요.
   (`async_insert`는 여전히 **기본값이 꺼져 있습니다** — opt-in입니다.)
3. **배경 병합 풀**: `background_pool_size`(기본 16),
   `background_merges_mutations_concurrency_ratio`(기본 2)는 최근
   버전에서는 코어 수에 맞춰 수동으로 스케일링할 필요가 예전보다 줄었지만,
   병합이 지속적으로 밀린다면(`system.parts`의 활성 파트 수가 계속 증가) 여전히
   튜닝 대상입니다 — GUIDE.md 12절의 "Parts by State" 패널로 이 추세를
   상시 관찰하세요.

## 5. 메모리/동시성 설정

- `max_server_memory_usage_to_ram_ratio`: 공유 호스트/K8s에서는 0.8 권장
  (기본 0.9).
- `max_memory_usage`(쿼리당, 기본 ~10GB): 사용자별로 맡기기보다, **기본
  프로파일에 안전한 상한을 명시적으로 설정**해 모든 사용자가 상속받게 하는 게
  베스트 프랙티스입니다. 개별 쿼리마다 설정을 강제하기보다 조직 차원의
  기본값을 정하세요.
- `max_concurrent_queries`: 동시 쿼리 수 상한. 무제한이면 쿼리 폭주 시 메모리
  압박으로 전체 서버가 흔들릴 수 있습니다.

## 6. 복제/샤딩 아키텍처 원칙

- **자동 리밸런싱은 없습니다.** 샤드를 늘려도 기존 데이터는 옛 해시 링에 그대로
  남아있고, 새 샤드는 처음엔 비어있습니다. 재분배가 필요하면 `INSERT SELECT`
  (Distributed 경유 재해싱) → 원본에서 `ALTER ... DELETE`의 수동 절차뿐입니다
  (GUIDE.md 13절에서 실측 — 3→4샤드 확장 후 기존 13004행이 새 샤드로 자동
  이동하지 않음을 확인). **샤드 확장은 스키마 마이그레이션급 작업으로
  취급하고 계획하세요.**
- **샤딩 키 선택이 곧 쿼리 성능**: `rand()`는 완벽히 균등하지만 샤드 로컬
  집계/조인의 이점을 못 씁니다. 실제로는 자주 함께 조회되는 데이터가 같은
  샤드에 있도록(예: `tenant_id`, `user_id` 해시) 설계하되, 특정 키에 트래픽이
  쏠리는 "핫 샤드"가 생기지 않는지 사전에 카디널리티를 검증하세요.
- **로드밸런싱 정책은 상황에 맞게**: `in_order`/`first_or_random`은 정상
  상태에서 동일하게 동작하지만 장애 시 갈립니다 — `in_order`는 장애 난 첫
  레플리카의 "다음 순번"에 트래픽이 몰리고, `first_or_random`은 살아있는
  나머지에 고르게 분산됩니다(GUIDE.md 8-5절 실측). 다중 레플리카 클러스터의
  기본값으로는 `first_or_random`이 대체로 더 안전합니다.

## 7. 백업 & 재해복구

- **레플리카는 백업이 아닙니다.** 복제는 하드웨어 장애를 버티게 해줄 뿐,
  실수로 지운 테이블이나 잘못된 mutation은 모든 레플리카에 그대로 전파됩니다.
- ClickHouse는 네이티브 `BACKUP`/`RESTORE` SQL 명령을 1급 지원합니다
  (`File()`, `Disk()`, `S3()`, `AzureBlobStorage()` 등 다양한 목적지 지원).
  커뮤니티 도구(`clickhouse-backup` 등)를 쓸 수도 있지만, 공식 문서는 특정
  서드파티 도구를 표준으로 못박지 않고 "만능 해법은 없다"며 **주기적으로
  별도 클러스터에 실제 복구 테스트를 해보라**고 강조합니다 — 백업 자체보다
  "복구가 실제로 되는지 검증"이 핵심입니다.
- Mutation(`ALTER ... UPDATE/DELETE`)은 비동기 mutation 큐를 거치므로 즉시
  반영되지 않습니다. 대량 삭제/수정 전에는 백업이 최신 상태인지, 그리고
  `mutations_sync`를 켜서 완료 시점을 명확히 통제할지 결정하세요(GUIDE.md
  16절).

## 8. 업그레이드/롤백 전략

> ⚠️ **다운그레이드는 지원되지 않습니다.** 새 버전이 온디스크 포맷(예: 시스템
> 로그 테이블의 마크 파일 포맷)을 한 번 쓰고 나면, 구버전이 그걸 파싱하지
> 못해 `ASYNC_LOAD_FAILED` 류 오류로 크래시루프에 빠집니다. **우리 랩에서
> 직접 재현했습니다**(GUIDE.md 14절, 26.8→24.8 다운그레이드 시도).

- 업그레이드는 **한 단계씩, 되돌릴 수 없다는 전제로** 계획하세요. 롤백
  계획이 "이전 버전 재설치"가 아니라 "백업에서 복구"여야 합니다.
- 오퍼레이터(Altinity)는 레플리카를 한 번에 1~2개씩 순차 교체합니다 — 이
  자체는 무중단을 지향하지만, 클라이언트가 특정 레플리카 하나에 고정
  연결(`port-forward` 등)돼 있으면 그 레플리카가 재시작될 때 마치 다운타임
  처럼 보일 수 있습니다. **실제 클라이언트는 로드밸런서/Service 경유로
  여러 레플리카에 걸쳐 붙어야 합니다** — 이는 우리가 처음 겪은 측정
  방법론의 함정이기도 합니다.
- `:latest`/`:head` 같은 이동 태그로 프로덕션을 운영하지 마세요. 재배포마다
  실제로 다른 빌드가 뜰 수 있습니다(우리 랩에서도 재구축 시
  `26.9.1.510` → `26.9.1.872`로 드리프트 확인). **항상 구체적인 버전
  태그로 고정**하세요.

## 9. 모니터링 & 알람 임계치

GUIDE.md 12절에서 만든 관측 스택(Prometheus + Grafana)과 `system.*` 치트시트를
그대로 프로덕션에 옮길 수 있습니다. 알람으로 격상해야 할 핵심 신호:

| 신호 | 정상 | 알람 조건 |
|---|---|---|
| `system.replicas.active_replicas` vs `total_replicas` | 같음 | 다르면 즉시 알람 (레플리카 다운) |
| `system.replicas.is_readonly` | 0 | 1 이상이면 즉시 알람 (Keeper 연결 문제) |
| `system.mutations`의 `is_done=0` 지속 시간 | 짧음 | 장시간 정체 시 알람 (배경 병합 문제) |
| `system.replication_queue` 건수 | 낮고 빠르게 소진 | 지속 증가 시 경고 (복제 지연) |
| 파티션당 활성 파트 수 | `parts_to_delay_insert`(1000) 아래 | 근접 시 경고, `parts_to_throw_insert`(3000) 근접 시 긴급 |
| Keeper `mntr`의 `zk_server_state` | 앙상블에 리더 1개 정확히 존재 | 리더 0개/2개 이상 관측 시 알람 |
| 디스크 여유 공간(`system.disks`) | 여유 충분 | 임계치(예: 20%) 이하 시 알람 — 파트 병합은 일시적으로 추가 공간을 필요로 함 |

## 10. 보안

- Keeper/ClickHouse 노드 간 통신에 TLS 적용 (특히 퍼블릭 네트워크 경유 시).
- 기본 `default` 사용자에 빈 비밀번호로 두지 말 것 — 우리 랩은 랩이라 익명
  접속을 허용했지만, 프로덕션에서는 반드시 사용자별 프로파일 + 쿼터 설정.
- 설정 파일의 비밀번호/토큰은 평문 대신 `<from_env>` 또는 시크릿 매니저 경유.

## 11. 쿠버네티스 배포 시 체크리스트

이 랩(kind + Altinity 오퍼레이터)에서 직접 겪은 것 기준:

- [ ] **PVC는 선택이 아니라 필수**: `dataVolumeClaimTemplate` 없이 배포하면
      파드 재시작만으로 데이터와 스키마가 전부 사라집니다(GUIDE.md 7-1/7-2절).
      프로덕션 StorageClass는 `local-path`가 아니라 실제 SSD 기반(예: 클라우드
      프로바이더의 io2/gp3급, 또는 로컬 NVMe PV)이어야 합니다.
- [ ] **Keeper 안티어피니티**: `podAntiAffinity`로 Keeper 파드가 서로 다른
      노드(가능하면 다른 AZ)에 뜨도록 강제.
- [ ] **리소스 requests/limits을 실측치 기준으로** — 부하테스트 없이 기본값을
      쓰면 코어 경합으로 병렬 처리 기능(`max_parallel_replicas` 등)이 오히려
      역효과를 낼 수 있습니다(GUIDE.md 15절).
- [ ] **이미지 태그 고정** (`:head`/`:latest` 금지).
- [ ] **PodDisruptionBudget** 설정으로 오퍼레이터의 롤링 업데이트나 노드
      드레인 시 한 번에 너무 많은 레플리카가 동시에 내려가지 않도록.
- [ ] **모니터링을 배포와 함께**: CHI에 Prometheus 익스포터 설정을 처음부터
      포함(신규 `config.d` 파일 추가는 파드 재시작을 유발하므로, 나중에
      추가하기보다 초기 배포에 포함하는 게 무중단 관점에서 유리 — GUIDE.md
      11절에서 확인).

---

## 요약: 프로덕션 배포 전 최종 체크리스트

1. Keeper 3(또는 5)노드, 데이터 노드와 분리, 안티어피니티 적용, NVMe 디스크
2. `ORDER BY`/파티션 키 설계 확정 (변경 비용이 크므로 사전 검증)
3. 인서트는 배치 우선, 안 되면 `async_insert=1,wait_for_async_insert=1`
4. 메모리/동시성 상한을 기본 프로파일에 명시
5. 백업은 네이티브 `BACKUP`/`RESTORE` + 정기 복구 리허설
6. 버전은 구체적 태그로 고정, 업그레이드는 단방향으로 계획(롤백=백업복구)
7. `system.*` + Prometheus/Grafana로 위 9절 표의 신호를 상시 알람화
8. K8s라면 PVC·안티어피니티·리소스 실측·PDB 네 가지를 배포 전 반드시 확인
